
&НаКлиенте
Процедура РедактироватьПрава(Команда)
	
	ПараметрыФормы 	= Новый Структура;
	НаборПрав 		= Неопределено;
	
	Для Каждого ЭлементОтбора Из Список.Отбор.Элементы Цикл
		
		Если ТипЗнч(ЭлементОтбора.ПравоеЗначение) = Тип("СправочникСсылка.Пользователи") Тогда
			НаборПрав = ЭлементОтбора.ПравоеЗначение;
			ПараметрыФормы.Вставить("НаборПрав",НаборПрав);
			Прервать;
		КонецЕсли;	
		
	КонецЦикла;
	
	Если НаборПрав = Неопределено Тогда
		ТекущиеДанные = Элементы.Список.ТекущиеДанные;
		
		Если ТекущиеДанные <> Неопределено Тогда
			НаборПрав = ТекущиеДанные.Пользователь;
			ПараметрыФормы.Вставить("НаборПрав",НаборПрав);
		КонецЕсли;
		
	КонецЕсли;
	ОткрытьФорму("РегистрСведений.ЗначенияПравПользователя.Форма.ФормаРедактирования",ПараметрыФормы );
КонецПроцедуры
