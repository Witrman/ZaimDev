#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.УправлениеДоступом

// См. УправлениеДоступомПереопределяемый.ПриЗаполненииСписковСОграничениемДоступа.
Процедура ПриЗаполненииОграниченияДоступа(Ограничение) Экспорт

КонецПроцедуры

// Конец СтандартныеПодсистемы.УправлениеДоступом

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура НачальноеЗаполнение () Экспорт
	
	ЗаполнитьСведенияОСтавкахОСМС();
	
КонецПроцедуры

Процедура ЗаполнитьСведенияОСтавкахОСМС() Экспорт
	
	НаборЗаписей = РегистрыСведений.СведенияОСтавкахОСМС.СоздатьНаборЗаписей();
	
	// ДоходыОтРаботодателя
	ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, '20170701', Перечисления.ЮрФизЛицо.ФизЛицо, 	0, 1);
	ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, '20180101', Перечисления.ЮрФизЛицо.ФизЛицо, 	0, 1.5);
	ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, '20200101', Перечисления.ЮрФизЛицо.ФизЛицо, 	1, 2);
	ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, '20210101', Перечисления.ЮрФизЛицо.ФизЛицо, 	2, 2);
	ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, '20220101', Перечисления.ЮрФизЛицо.ФизЛицо, 	2, 3);
    
    // ДоходыОтНалоговогоАгента
	ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, '20170701', Перечисления.ЮрФизЛицо.ЮрЛицо, 5, 0);
	ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, '20180101', Перечисления.ЮрФизЛицо.ЮрЛицо, 0, 0);
	ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, '20200101', Перечисления.ЮрФизЛицо.ЮрЛицо, 1, 0);
	ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, '20210101', Перечисления.ЮрФизЛицо.ЮрЛицо, 2, 0);
	
	Попытка
		НаборЗаписей.Записать(Истина);
	Исключение
		ТекстСообщения = НСтр("ru='Операция не выполнена'");
		ЗаписьЖурналаРегистрации(ТекстСообщения, УровеньЖурналаРегистрации.Ошибка,,, ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
	КонецПопытки;
	
КонецПроцедуры

Процедура ДобавитьСведенияОСтавкахОСМС(НаборЗаписей, Период, ВидДохода,СтавкаВзносы,СтавкаОтчисления)
	
   	НоваяЗапись = НаборЗаписей.Добавить();
	НоваяЗапись.Период = Период;  
	НоваяЗапись.ВидДохода = ВидДохода;
	НоваяЗапись.СтавкаВзносы = СтавкаВзносы;
	НоваяЗапись.СтавкаОтчисления = СтавкаОтчисления;
	
КонецПроцедуры	

#КонецОбласти

#КонецЕсли