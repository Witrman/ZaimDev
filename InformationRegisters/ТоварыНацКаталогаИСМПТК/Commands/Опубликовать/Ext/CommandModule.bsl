
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	//Должна срабатывать только для карточек в статусе Перечисление.СтатусыКарточекНацКаталогаИСМПТК.ОжидаетПодписания
	//Для остальных должно быть сообщение, что действие невозможно
	//Доступ к команде: в формах списка и записи регистра. 
	//При этом в форме записи в УправлениеФормой прописывается видимость этой команды только для статуса ОжидаетПодписания (т.е. потребуется вызов процедуры в оповещении, если выполнялась команда обновления и статус карточки изменился)
	//После публикации нужно дозаполнять поле ДатаПубликации
	
	Тест = Истина;
	
КонецПроцедуры
