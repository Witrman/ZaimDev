
Процедура ПередЗаписью(Отказ, Замещение)
	
	Для Каждого Запись Из ЭтотОбъект Цикл
		
		ДанныеЗаполнены = Истина;
		
		Если Не ЗначениеЗаполнено(Запись.Организация) Тогда
			ДанныеЗаполнены = Ложь;
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Запись.ВидПродукции) Тогда
			ДанныеЗаполнены = Ложь;
		КонецЕсли;
		Если Не ЗначениеЗаполнено(Запись.Номенклатура) Тогда
			ДанныеЗаполнены = Ложь;
		КонецЕсли;
		
		Запись.КарточкаЗаполнена = ДанныеЗаполнены;
		
	КонецЦикла;
	
КонецПроцедуры
